import CreateForm from "./CreateForm";

export default function CreateEmployee(){
    return(
        <main>
            <h2 className='text-primary text-center'>Add a New Employee</h2>
            <CreateForm />
        </main>
    )
}